- use native powerprofilesdaemon module in waybar, feature parity should exist, colors should work via css, use github docs
- remove animations and borders and all the visual fluff, every cpu cycle counts
- create an iso creator, either as a separate repo or right inside this repo: a NixOS live ISO shipping the tools + aliases I need (disko to mount drives, easy generation creation, etc.), serving as both an installer on a fresh machine and a rescue toolbelt for a broken install — ideally with a command that runs the README install steps for me. Research/design already written in `ISO_PLAN.md`. it should build via nix build imo something like nix build .#iso
