# Usage
Run the following the commands to use this configuration on a fresh nixos system

1. `nix-shell -p git --command "git clone https://github.com/sean-imus/nixos-config.git ~/nixos-config"
2. `./install.sh`
