# Usage
Run the following the commands to use this configuration on a fresh nixos system

1.
```
nix-shell -p git bash
```
2. 
```
git clone https://github.com/sean-imus/nixos-config.git ~/nixos-config"
```
3. 
```
./nixos-config/install.sh
```
